{
  "name": "cv-generator",
  "version": "1.0.0",
  "description": "Generate professional CVs from Markdown files",
  "type": "module",
  "scripts": {
    "preview": "node src/build.js --preview",
    "generate": "node src/build.js",
    "generate:all": "node src/build.js --all",
    "clean": "rm -rf output/*.pdf output/*.html",
    "watch": "node src/build.js --watch"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0",
    "playwright": "^1.41.0"
  },
  "devDependencies": {
    "chokidar": "^3.5.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
